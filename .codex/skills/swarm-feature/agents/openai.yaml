interface:
  display_name: Swarm Feature
  short_description: >
    Multi-agent swarm for designing and implementing new features.
    Independent parallel investigation, structured synthesis,
    implementation in a code worktree, and multi-agent review.
  default_prompt: >
    Use $swarm-feature for this task: Multi-agent swarm for designing and
    implementing new features. Independent parallel investigation,
    structured synthesis, implementation in a code worktree, and multi-
    agent review.

policy:
  allow_implicit_invocation: true
